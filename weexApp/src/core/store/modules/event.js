import event from '../../net/event'

// initial state
// shape: [{ id, quantity }]
const state = {
    received_events_data_list: [],
};

// getters
const getters = {};

// actions
const actions = {

    
};

// mutations
const mutations = {

};

export default {
    state,
    getters,
    actions,
    mutations
}